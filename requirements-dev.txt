# Development-only dependencies.
# Runtime dependencies should come from:
# 1) conda environment.yml (authoritative on Windows / RDKit)
# 2) pyproject.toml optional dependency groups when pip layering is desired

pytest>=8.3,<9
pytest-cov>=5,<6
ruff>=0.6,<0.7
black>=24.8,<25
mypy>=1.11,<2
pre-commit>=3.8,<4
pip-tools>=7.4,<8
types-PyYAML>=6.0.12,<7

